# Language and framework preference established

User wants all code examples and lessons to use Java with Spring Boot and reactive programming (Project Reactor / WebFlux). This applies to all future lessons, reference materials, and code snippets.

**Implications**: Use the Azure SDK for Java (`com.azure:azure-messaging-servicebus`), the Spring Cloud Azure starter (`com.azure.spring:spring-cloud-azure-starter-servicebus`), and reactive patterns (Mono, Flux) throughout. The .NET-focused glossary and lesson 1 content is still valid conceptually — only the code implementation language changes.
